# Portfolio Monorepo

This scaffold contains a client (React) and server (Express) app.

- client: React frontend
- server: Express backend

To run locally:
- Install dependencies in each workspace (`client` and `server`) and start them individually.
